/**
 * Height utilities for virtualized lists
 * Provides constants and utilities for different virtualized list types
 */

// Height for conversation list items (title + date + padding)
// Based on ConversationDrawer item: p-4 padding + text content
export const CONVERSATION_ITEM_HEIGHT = 73;

// Default height estimation for messages (if needed in future)
export const DEFAULT_MESSAGE_HEIGHT = 150;
